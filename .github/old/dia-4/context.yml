name: Mostrar contextos
on:
  workflow_dispatch
jobs:
  show-context:
    runs-on: ubuntu-latest
    steps:
      - name: Mostar Github contexto
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"