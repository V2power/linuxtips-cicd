name: First workflow
on:
  workflow_dispatch:

jobs:
  giropops:
    runs-on: ubuntu-latest
    steps:
      - name: Confere versao do Ubuntu
        run: cat /etc/os-release

      - name: Exibir uma mensagem
        run: echo "LINUXtips"