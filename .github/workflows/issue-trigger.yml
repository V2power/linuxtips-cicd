name: Testando trigger de Issue
on:
  issues:
    types: [opened, edited, deleted]
jobs:
  job-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Print Issue
        run: echo "Uma issue foi aberta/editada/deletada"