name: Testando a regra de Create
on:
  create
jobs:
  print-branch-or-tag-creation:
    runs-on: ubuntu-latest
    steps:
      - name: Printa a Branch or Tag
        run: echo "Uma nova branch ou tag foi criada no repositório"