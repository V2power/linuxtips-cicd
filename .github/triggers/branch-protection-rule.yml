name: Branch Protection Rule

on:
  branch_protection_rule:
    types: [created, deleted]

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Avisa sobre modificacao
        run: echo "Este workflow foi triggado pois uma regra de Branch Protection Rule foi ativada."