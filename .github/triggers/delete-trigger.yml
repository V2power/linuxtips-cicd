name: Testando a regra de Delete
on:
  delete
jobs:
  print-branch-or-tag-creation:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo "Uma nova branch ou tag foi deletada no repositório"